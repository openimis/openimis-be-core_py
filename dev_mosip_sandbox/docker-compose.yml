version: '3.8'
services:
  mosip-mock:
    build: ./mosip-mock
    ports:
      - "5000:5000"
    environment:
      KEYCLOAK_BASE: http://host.docker.internal:8080
      KEYCLOAK_REALM: openimis
    networks:
      - mosipnet

networks:
  mosipnet:
    driver: bridge
